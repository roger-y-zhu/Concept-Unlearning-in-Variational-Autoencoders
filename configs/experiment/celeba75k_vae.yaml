name: celeba75k_vae
model: vae
dataset: celeba

overrides:
  dataloader:
    subset: 75000
  model:
    latent_dim: 128
  training:
    epochs: 120
    lr: 0.0001
    kl_max_weight: 0.08
    kl_warmup_epochs: 25